export default class language {
  code: string;
  name: string;
  font: string;

  constructor(code: string, name: string, font: string) {
    this.code = code;
    this.name = name;
    this.font = font;
  }
}
